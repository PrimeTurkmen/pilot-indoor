{
  "name": "pilot-indoor-positioning-engine",
  "version": "3.0.0",
  "description": "ELA/Wirepas mesh + Channel Sounding — Production-grade indoor positioning (ported from SiteTrack)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "MOCK_DATA=1 node server.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "mqtt": "^5.3.0",
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
